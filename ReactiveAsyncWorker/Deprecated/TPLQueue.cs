using ReactiveAsyncWorker.Model;
using System;
using System.Reactive.Concurrency;


namespace ReactiveAsyncWorker.ViewModel
{
    //public class TPLQueue<T> : AsyncWorkerBaseQueue<TaskItem<T>>
    //{
    //    public TPLQueue(IScheduler scheduler) : base(scheduler)
    //    {
    //    }

    //    protected override IObservable<ProgressState> Convert(IObservable<TaskItem<T>> newitems)
    //    {
    //        return new DataFlowQueue<T>(newitems, new System.Threading.CancellationTokenSource());
    //    }
    //}
}
