<UserControl x:Class="Utility.Trees.Demo.MVVM.Views.PipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Utility.Trees.Demo.MVVM"        
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:conv="http://schemas.utility.com/converters"
             xmlns:common="clr-namespace:Utility.Trees.Demo.MVVM"
             d:DataContext="{d:DesignInstance common:PipeController}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <FrameworkElement.Resources>

        <ResourceDictionary>
            <!--<ResourceDictionary.MergedDictionaries>
                <res:SharedResourceDictionary Source="/Puttshack.Views.Templates;component/Themes/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>-->


            <Style x:Key="CustomRadioStyle"  TargetType="{x:Type RadioButton}">
                <Setter Property="Foreground" Value="#373737" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Width" Value="40" />
                <Setter Property="Height" Value="40" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="Background" Value="LightGray" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                <Grid>
                                    <ContentPresenter  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" />
                                </Grid>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E59400" />
                                    <Setter Property="Foreground" Value="Black" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="LightSeaGreen" />
                                    <Setter Property="Foreground" Value="Black" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Foreground" Value="LightGray" />
                                    <Setter Property="Background" Value="White" />
                                    <!--<Setter Property="Background" Value="DarkSeaGreen" />-->
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter Property="Foreground" Value="Black" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style  x:Key="CustomButtonStyle"  TargetType="{x:Type Button}">
                <Setter Property="Foreground" Value="#373737" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Width" Value="40" />
                <Setter Property="Height" Value="40" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Margin" Value="2" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                <Grid>
                                    <ContentPresenter  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E59400" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="LightSeaGreen" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <conv:TypeToNameConverter x:Key="TypeNameConverter"></conv:TypeToNameConverter>

            <DataTemplate DataType="{x:Type local:QueueItem}">
                <Grid    TextBlock.FontSize="12">
                    <Grid.ColumnDefinitions>
                        <!--<ColumnDefinition Width="0"></ColumnDefinition>
                        <ColumnDefinition Width="20"></ColumnDefinition>

                        <ColumnDefinition Width="150"></ColumnDefinition>-->
                        <!--<ColumnDefinition Width="30"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>-->

                    </Grid.ColumnDefinitions>

                    <!--<TextBlock Grid.Column="2"   Text="{Binding Type, Converter={StaticResource TypeNameConverter}}"></TextBlock>-->
            
                    <UniformGrid  Rows="1">
                        <TextBlock Text="{Binding Guid}"></TextBlock>
                        <TextBlock Text="{Binding Name}"></TextBlock>
                        <TextBlock Text="{Binding Index}"></TextBlock>
                        <TextBlock Text="{Binding Type, Converter={StaticResource TypeNameConverter}}"></TextBlock>
                        <!--<TextBlock Text="{Binding Old.Value}"></TextBlock>-->
                    </UniformGrid>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </FrameworkElement.Resources>

    <DockPanel>
        <WrapPanel Height="60" DockPanel.Dock="Top" HorizontalAlignment="Center">
            <RadioButton x:Name="Play"  GroupName="PlayPause" Style="{StaticResource CustomRadioStyle}"  IsChecked="{Binding IsPlaying}">
                <iconPacks:PackIconZondicons Kind="Play" />
            </RadioButton>
            <RadioButton x:Name="Pause" GroupName="PlayPause" Style="{StaticResource CustomRadioStyle}" IsChecked="{Binding IsPaused}">
                <iconPacks:PackIconZondicons Kind="Pause" />
            </RadioButton>
            <Button x:Name="Step" Style="{StaticResource CustomButtonStyle}"  Command="{Binding StepCommand}">
                <Viewbox Margin="8">
                    <iconPacks:PackIconZondicons Kind="ArrowRight" />
                </Viewbox>
            </Button>
        </WrapPanel>
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="80"></RowDefinition>
                <RowDefinition Height="8"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <UniformGrid Grid.Column="0" Rows="1">
                <TextBlock Text="Guid" FontWeight="DemiBold"></TextBlock>
                <TextBlock Text="Name" FontWeight="DemiBold"></TextBlock>
                <TextBlock Text="Index" FontWeight="DemiBold"></TextBlock>
                <TextBlock Text="Type" FontWeight="DemiBold"></TextBlock>
            </UniformGrid>
            <ScrollViewer Grid.Row="1">
                <ItemsControl  ItemsSource="{Binding Source={x:Static common:Pipe.Instance}, Path=Completed}"/>
            </ScrollViewer>

            <Border Background="#7FFF8000"  Grid.Row="2">
                <ContentControl Content="{Binding Next}" TextBlock.FontWeight="Bold" TextBlock.FontSize="18">

                </ContentControl>
            </Border>
            <ItemsControl  Grid.Row="4"  ItemsSource="{Binding Source={x:Static common:Pipe.Instance}, Path=Pending}"/>
        </Grid>
    </DockPanel>

</UserControl>
