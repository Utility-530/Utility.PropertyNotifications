namespace Utility.Enums.VisualElements
{
    public enum ReactNative
    {
        View, Text, TextInput, ScrollView, FlatList, SectionList, Image,
        Button, Switch, Picker, Modal, TouchableOpacity, Slider,
        RadioButton,
        ActivityIndicator,
        Audio,
        Video
    }
}
