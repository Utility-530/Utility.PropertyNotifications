namespace Utility.Enums.VisualElements
{
    public enum Flutter
    {
        Container, Column, Row, Text, TextField, ListView, GridView, Slider,
        Checkbox, Radio, ElevatedButton, TabBarView, Image, AudioPlayer,
        VideoPlayer, ScrollView,
        DropdownButton,
        ProgressIndicator
    }
}
