namespace Utility.Enums.VisualElements
{
    public enum JavaFX
    {
        Pane, Text, TextField, TextArea, CheckBox, RadioButton, ComboBox,
        Button, ListView, TableView, TreeView, TabPane, ProgressBar,
        ImageView, MediaView,
        PasswordField,
        Slider
    }
}
