<UserControl x:Class="Utility.WPF.Demo.Buttons.PlayBackUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Utility.WPF.Demo.Buttons" xmlns:btn="http://schemas.utility.com/buttons"
             mc:Ignorable="d"
             d:Background="White"
             d:DataContext="{d:DesignInstance local:PlayBackViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel>
            <btn:PlayBackButtons Last="{Binding Last, Mode=TwoWay}"></btn:PlayBackButtons>
        </WrapPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Collection}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Content" Value="{Binding Data}"></Setter>
                    <Setter Property="IsSelected" Value="{Binding IsSelected}"></Setter>
                    <Setter Property="Height" Value="120"></Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="FontSize" Value="48" />
                            <Setter Property="Background" Value="DarkSeaGreen" />
                            <Setter Property="BorderBrush" Value="DarkSeaGreen" />
                            <Setter Property="BorderThickness" Value="4" />
                            <Setter Property="Foreground" Value="Peru" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>