<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Utility.WPF.Controls.Objects"
    xmlns:templates="http://schemas.utility.com/templates"
    xmlns:conv="http://schemas.utility.com/converters">

    <Style TargetType="{x:Type local:ObjectSelector}" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ObjectSelector}">
                    <StackPanel>
                        <Viewbox HorizontalAlignment="Stretch">
                            <local:ObjectItemsControl
                                x:Name="ObjectItemsControl"
                                Orientation="Horizontal" />
                        </Viewbox>
                        <ContentControl
                                        Content="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}"
                                        ContentTemplateSelector="{x:Static templates:CustomDataTemplateSelector.Instance}"
                                        Margin="2" 
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"  
                                        />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>