<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Utility.Simulation;assembly=Utility.Simulation" 
                    xmlns:btn="http://schemas.utility.com/buttons">


    <DataTemplate DataType="{x:Type local:PlayBackViewModel}">
        <Viewbox>
            <btn:PlayBackButtons Last="{Binding Last, Mode=TwoWay}" Enabled="{Binding Enabled, Mode=OneWay}"></btn:PlayBackButtons>
        </Viewbox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:HistoryViewModel}">
        <ListBox ItemsSource="{Binding Collection}" VerticalAlignment="Stretch" >
    
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    
                    <!-- to prevent user selection -->
                    <Setter Property="Focusable" Value="False"/>                 
                    <Setter Property="IsHitTestVisible" Value="False"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <!--to prevent user selection -->

                    <Setter Property="Content" Value="{Binding Data, Mode=OneTime}"></Setter>

                    <Setter Property="IsSelected" Value="{Binding IsSelected}"></Setter>
                    <Setter Property="MaxHeight" Value="120"></Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True" >
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="FontSize" Value="48" />
                            <Setter Property="Background" Value="DarkSeaGreen" />
                            <Setter Property="BorderBrush" Value="DarkSeaGreen" />
                            <Setter Property="BorderThickness" Value="4" />
                            <Setter Property="Foreground" Value="Peru" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:MasterPlayViewModel}">
        <ItemsControl ItemsSource="{Binding Collection}" >
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="{x:Type ContentPresenter}">
                    <Setter Property="Grid.Row" Value="{Binding GridRow}"></Setter>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

    </DataTemplate>
    
</ResourceDictionary>