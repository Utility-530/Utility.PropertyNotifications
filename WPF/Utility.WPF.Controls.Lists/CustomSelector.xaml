<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:btns="http://schemas.utility.com/buttons"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:res="http://schemas.utility.com/resourcedictionary"
                    xmlns:c="http://schemas.utility.com/colours"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
                    xmlns:ext="http://schemas.utility.com/external" 
                    xmlns:att="http://schemas.utility.com/attached" 
                    xmlns:beh="http://schemas.utility.com/behaviors" 
                    xmlns:obj="http://schemas.utility.com/objects" 
                    xmlns:local="clr-namespace:Utility.WPF.Controls.Lists"
                    xmlns:conv="http://schemas.utility.com/converters"
                    >

    <ResourceDictionary.MergedDictionaries>
        <res:SharedResourceDictionary Source="/Utility.WPF.Geometries;component/Resources.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--<local:CheckedEventArgsConverter x:Key="CheckedEventArgsConverter"/>-->
    <obj:ObjectToTokenConverter x:Key="ObjectToTokenConverter"/>
    <local:ItemsSourceConverter x:Key="ItemsSourceConverter"/>
    <local:NullTo40Converter x:Key="NullTo40Converter"/>
    <conv:NullToInverseVisibilityConverter x:Key="NullToInverseVisibilityConverter"/>
    <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>


    <Style x:Key="ButtonFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <LinearGradientBrush x:Key="ButtonNormalBackground" EndPoint="0,1" StartPoint="0,0">
        <GradientStop Color="#F3F3F3" Offset="0"/>
        <GradientStop Color="#EBEBEB" Offset="0.5"/>
        <GradientStop Color="#DDDDDD" Offset="0.5"/>
        <GradientStop Color="#CDCDCD" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ButtonNormalBorder" Color="#FF707070"/>

    <SolidColorBrush x:Key="CheckBoxFillNormal" Color="#F4F4F4"/>
    <SolidColorBrush x:Key="CheckBoxStroke" Color="#8E8F8F"/>
    <Style x:Key="EmptyCheckBoxFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="1" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CheckRadioFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="14,0,0,0" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CheckBoxPingUnPing" TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="Background" Value="{StaticResource CheckBoxFillNormal}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CheckBoxStroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Width" Value="20"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource EmptyCheckBoxFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Border x:Name="border" BorderBrush="{c:Amber 7}" BorderThickness="4" CornerRadius="4">
                        <Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="Black" Offset="0"/>
                                <GradientStop Color="Black" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Checked">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[0].(GradientStop.Color)" Storyboard.TargetName="border">
                                            <EasingColorKeyFrame KeyTime="0" Value="#FF0073FD"/>
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[1].(GradientStop.Color)" Storyboard.TargetName="border">
                                            <EasingColorKeyFrame KeyTime="0" Value="#FF005BC2"/>
                                        </ColorAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[1].(GradientStop.Offset)" Storyboard.TargetName="border">
                                            <EasingDoubleKeyFrame KeyTime="0" Value="0.742"/>
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[0].(GradientStop.Offset)" Storyboard.TargetName="border">
                                            <EasingDoubleKeyFrame KeyTime="0" Value="0.143"/>
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Unchecked"/>
                                <VisualState x:Name="Indeterminate"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <att:InteractivitySetter Property="att:InteractivityItems.Template">
            <Setter.Value>
                <att:InteractivityTemplate>
                    <att:InteractivityItems>
                        <att:InteractivityItems.Triggers>
                            <i:EventTrigger EventName="Checked">
                                <beh:InvokeCommandAction PassEventArgsToCommand="True"  Command="{Binding CheckedCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
                            </i:EventTrigger>
                            <i:EventTrigger EventName="Unchecked">
                                <beh:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding CheckedCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
                            </i:EventTrigger>
                        </att:InteractivityItems.Triggers>
                    </att:InteractivityItems>
                </att:InteractivityTemplate>
            </Setter.Value>
        </att:InteractivitySetter>
    </Style>

    <Style x:Key="ButtonClose" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
        <Setter Property="Background" Value="{StaticResource ButtonNormalBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{c:Blue}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Width" Value="20"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Command" Value="{Binding RemoveCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="CommandParameter" Value="{Binding DataContext, RelativeSource={RelativeSource Self}}"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border BorderBrush="Transparent" BorderThickness="4" Background="Black" CornerRadius="3">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver"/>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="rectangle">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Unfocused"/>
                                <VisualState x:Name="Focused"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="ValidationStates">
                                <VisualState x:Name="Valid"/>
                                <VisualState x:Name="InvalidFocused"/>
                                <VisualState x:Name="InvalidUnfocused"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid>
                            <Path Data="M14.833333,10 L29,21.166667" Fill="{TemplateBinding Foreground}" Height="Auto" Stretch="Fill" Stroke="{TemplateBinding Foreground}" VerticalAlignment="Stretch" StrokeThickness="4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                            <Path Data="M7.041,20.209 L18.6875,5.75" Fill="{TemplateBinding Foreground}" HorizontalAlignment="Stretch" Stretch="Fill" Stroke="{TemplateBinding Foreground}" Width="Auto" StrokeThickness="4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                            <Rectangle x:Name="rectangle" Fill="#FF717172" Height="Auto" Stroke="{TemplateBinding Foreground}" StrokeThickness="0" VerticalAlignment="Stretch" Width="Auto" Opacity="0.485" RadiusX="0" RadiusY="0" Visibility="Hidden" ClipToBounds="True"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="true"/>
                        <Trigger Property="ToggleButton.IsChecked" Value="true"/>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="#ADADAD"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="ListBoxItemStyle1" TargetType="ListBoxItem">
        <!--<Setter Property="Foreground" Value="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>-->
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="ContextMenu">
            <Setter.Value>
                <ContextMenu>
                    <MenuItem Command="ApplicationCommands.Copy"></MenuItem>
                </ContextMenu>
            </Setter.Value>
        </Setter>
        <!--<Setter Property="BorderThickness" Value="{DynamicResource {x:Static adonisUi:Dimensions.BorderThickness}}"/>-->
        <!--<Setter Property="adonisExtensions:CornerRadiusExtension.CornerRadius" Value="{DynamicResource {x:Static adonisUi:Dimensions.CornerRadius}}"/>-->
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid>
                        <Border x:Name="Border"
                                CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Panel.ZIndex="0"/>
                        <Border x:Name="SpotlightLayer"    
                                CornerRadius="10"
                                BorderThickness="{TemplateBinding BorderThickness}"                    
                                SnapsToDevicePixels="False"
                                Panel.ZIndex="1"/>          

                        <GridViewRowPresenter Content="{TemplateBinding Content}" 
                                x:Name="GridViewRowPresenter"
                                Columns="{Binding Columns, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:CustomSelector}}}"                        >
                        </GridViewRowPresenter>
                                                
                        <btns:DuplicateGeometryButton 
                            x:Name="PlusButton"
                            Height="20"
                            Command="{Binding DuplicateCommand, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}, Mode=FindAncestor,AncestorLevel=1}}"
                            CommandParameter="{Binding DataContext, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                            VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,62,0" />
                        <Button Style="{StaticResource ButtonClose}"  Panel.ZIndex="3" HorizontalAlignment="Right" Height="20" Margin="{Binding ElementName=GridViewRowPresenter, Path=Columns, Converter={StaticResource NullTo40Converter} }" Width="20"  VerticalAlignment="Top" />
                        <CheckBox Style="{StaticResource CheckBoxPingUnPing}"  Panel.ZIndex="4" HorizontalAlignment="Right"  VerticalAlignment="Bottom"/>
                    </Grid>
                </ControlTemplate>
                
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=IsGrid, RelativeSource={RelativeSource Mode=FindAncestor,AncestorLevel=1, AncestorType={x:Type local:CustomSelector}}}" Value="False">
                <Setter Property="Template" >
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <Grid>
                                <Border x:Name="Border"
                                CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Panel.ZIndex="0"/>
                                <Border x:Name="SpotlightLayer"    
                                           CornerRadius="10"
                                BorderThickness="{TemplateBinding BorderThickness}"                    
                                SnapsToDevicePixels="False"
                                Panel.ZIndex="1"/>
                                <ContentPresenter                              
                                          RecognizesAccessKey="True"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Panel.ZIndex="2">

                                </ContentPresenter>


                                <btns:DuplicateGeometryButton 
                                    x:Name="PlusButton"
                                    Height="20"
                                    Command="{Binding DuplicateCommand, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}, Mode=FindAncestor,AncestorLevel=1}}"
                                    CommandParameter="{Binding DataContext, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                    VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,62,0" />
                                <Button Style="{StaticResource ButtonClose}"  Panel.ZIndex="3" HorizontalAlignment="Right" Height="20" Margin="{Binding ElementName=GridViewRowPresenter, Path=Columns, Converter={StaticResource NullTo40Converter} }" Width="20"  VerticalAlignment="Top" />
                                <CheckBox Style="{StaticResource CheckBoxPingUnPing}"  Panel.ZIndex="4" HorizontalAlignment="Right"  VerticalAlignment="Bottom"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                    <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="False" />
                </MultiDataTrigger.Conditions>
                <MultiDataTrigger.Setters>
                    <Setter Property="Background" Value="{c:Red 7}"/>
                    <Setter Property="BorderBrush" Value="{c:Blue 7}"/>
                    <!--<Setter Property="adonisExtensions:CursorSpotlightExtension.BackgroundBrush" Value="{DynamicResource {x:Static adonisUi:Brushes.Layer1IntenseHighlightBrush}}"/>
                    <Setter Property="adonisExtensions:CursorSpotlightExtension.BorderBrush" Value="{DynamicResource {x:Static adonisUi:Brushes.Layer1IntenseHighlightBorderBrush}}"/>-->
                </MultiDataTrigger.Setters>
            </MultiDataTrigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{c:Amber 7}"/>
                <Setter Property="BorderBrush" Value="{c:Green 7}"/>
                <Setter Property="Foreground" Value="{c:BlueGrey 7}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{c:Orange 7}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsEnabled" Value="False"/>
                    <Condition Property="IsSelected" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Foreground" Value="{c:Pink 7}"/>
            </MultiTrigger>
        
        </Style.Triggers>
    </Style>

    <Style x:Key="GridViewColumnHeaderStyle"  TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                    <Border BorderThickness="0,0,0,0" BorderBrush="Black" Background="Transparent">
                        <TextBlock x:Name="ContentHeader" Text="{TemplateBinding Content}" Padding="5,5,5,0" Width="{TemplateBinding Width}" TextAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style TargetType="{x:Type local:CustomSelector}" >
        <Style.Resources>
            <DataTemplate x:Key="JsonTemplate">
                <Grid Background="#DCDCDC" Name="rootElement">
                    <Border BorderBrush="Beige" BorderThickness="10">

                        <obj:JsonControl>
                            <i:Interaction.Behaviors>
                                <obj:ObjectBehavior Object="{Binding}"></obj:ObjectBehavior>
                            </i:Interaction.Behaviors>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="ValueChanged" >
                                    <beh:UpdateSourceBindingAction Property="{x:Static obj:JsonControl.ObjectProperty}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </obj:JsonControl>

                    </Border>
                </Grid>
                <!--<pt:PropertyTree Object="{Binding Path=.}"></pt:PropertyTree>-->
            </DataTemplate>
            <DataTemplate x:Key="ItemTemplate">
                <Grid Background="#DCDCDC" Name="rootElement">
                    <obj:JsonControl>
                        <i:Interaction.Behaviors>
                            <obj:ObjectBehavior Object="{Binding}"></obj:ObjectBehavior>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="ValueChanged" >
                                <beh:UpdateSourceBindingAction Property="{x:Static obj:JsonControl.ObjectProperty}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </obj:JsonControl>
                </Grid>
            </DataTemplate>

            <local:ItemTemplateSelector x:Key="ItemTemplateSelector"  DefaultTemplate="{StaticResource ItemTemplate}"/>
        </Style.Resources>
        <Setter Property="EditTemplate" Value="{StaticResource JsonTemplate}"></Setter>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ListBoxItemStyle1}"/>
        <!--<Setter Property="ItemContainerStyleSelector" Value="{StaticResource CustomContainerStyleSelector}"></Setter>-->
        <Setter Property="ItemTemplateSelector" Value="{StaticResource ItemTemplateSelector}"></Setter>
        <Setter Property="Edit">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource ItemsSourceConverter}">
                    <Binding Path="ItemsSource" RelativeSource="{RelativeSource Mode=Self}"></Binding>
                    <Binding Path="Edit" RelativeSource="{RelativeSource Mode=Self}"></Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>

                    <ext:DraggAnimatedPanel ItemSeparation="2" ItemsHeight="{Binding ItemsHeight, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}" 
                                            ItemsWidth="{Binding ItemsWidth, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}"
                                            Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}" 
                                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}"
                                            SwapCommand="{Binding SwapCommand, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SnapsToDevicePixels"          Value="true" />
        <!--<Setter Property="OverridesDefaultStyle"
          Value="true" />-->
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
          Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"
          Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll"
          Value="true" />
        <Setter Property="MinWidth"
          Value="120" />
        <Setter Property="MinHeight"
          Value="20" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomSelector}">
                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <Grid x:Name="DropDown" 
                              Grid.Row="1" 
                              VerticalAlignment="Top"
                              SnapsToDevicePixels="True"                        
                              MinWidth="{Binding ActualWidth}"
                              >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>

                            <Border x:Name="HeaderBorder" Background="Transparent" Visibility="{TemplateBinding Columns, Converter={StaticResource NullToInverseVisibilityConverter}}">
                                <!--GridViewRowPrsenter containing the current information.-->
                                <GridViewHeaderRowPresenter
                                                            ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}"
                                    AllowsColumnReorder="{TemplateBinding AllowsColumnReorder}" 
                                    Columns="{TemplateBinding Columns}" />

                            </Border>

                            <ext:AnimatedScrollViewer
								x:Name="PART_AnimatedScrollViewer"
								Padding="{TemplateBinding Padding}"
								Focusable="false"
                                Grid.Row="1"
								ScrollingSpline="0,0.50,0.50,1">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ext:AnimatedScrollViewer>
                            <!--<Grid DataContext="{TemplateBinding Columns}">
                                <TextBlock Grid.Row="1" Text="{Binding Length}" FontSize="100"></TextBlock>
                                <TextBlock Grid.Row="1" Text="{Binding Count}" FontSize="100"></TextBlock>

                            </Grid>-->

                            <Border x:Name="Border" Grid.Row="2" Background="LightGray" Margin="4">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition x:Name="row1" Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter x:Name="content_control" 
                                                    ContentSource="Edit">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type ContentPresenter}">
                                                <Setter Property="Opacity" Value="0"></Setter>
                                                <Style.Triggers>
                                                    <!--<DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                                <Setter Property="Opacity" Value="0.5" />
                                                                </DataTrigger>-->
                                                    <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                        <!--<Setter Property="Height" Value="0" />-->
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.1"></DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1"></DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </FrameworkElement.Style>
                                    </ContentPresenter>

                                    <btns:GeometryToggleButton x:Name="PlusButton"                                   
                                            Data="{StaticResource Plus}"
                                            Foreground="Black"      
                                            Width="{Binding ElementName=Border, Path=ActualWidth}"
                                            Grid.Row="1"                                                                           
                                            HorizontalAlignment="Stretch" 
                                            VerticalAlignment="Stretch" Margin="2" Padding="2">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type btns:GeometryToggleButton}">
                                                <Style.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Height" Value="0" />
                                                        <Setter Property="Width" Value="0" />
                                                    </Trigger>
                                                    <Trigger Property="IsChecked" Value="False">
                                                        <Setter Property="Height" Value="24" />
                                                        <Setter Property="Width" Value="24" />
                                                    </Trigger>


                                                </Style.Triggers>
                                            </Style>

                                        </FrameworkElement.Style>
                                    </btns:GeometryToggleButton>


                                    <WrapPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.Row="1" Margin="2">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type WrapPanel}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                        <Setter Property="Height" Value="0" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </FrameworkElement.Style>
                                        <btns:GeometryButton x:Name="Accept_Button" Data="{StaticResource Tick_Rounded}"   
                                                                         Width="32" Margin="2"
                                                                         Foreground="White"
                                                                         Background="{c:Green 7}"
                                                                         Height="32" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:ChangePropertyAction TargetName="PlusButton" PropertyName="IsChecked" Value="False"></i:ChangePropertyAction>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </btns:GeometryButton>
                                        <btns:GeometryButton x:Name="Decline_Button"   Data="{StaticResource Cancel_Rounded}"   
                                                                         Width="32" Margin="2"
                                                                         Foreground="White"
                                                                         Background="{c:Red 7}"
                                                                         Height="32" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:ChangePropertyAction TargetName="PlusButton" PropertyName="IsChecked" Value="False"></i:ChangePropertyAction>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </btns:GeometryButton>
                                    </WrapPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=IsChecked, ElementName=PlusButton}" Value="False">
                            <Setter TargetName="content_control" Property="Height" Value="0" />
                        </DataTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsGrid" Value="True">
                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <StackPanel></StackPanel>
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>


</ResourceDictionary>