<UserControl x:Class="Utility.WPF.Demo.Trees.DragablzUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Utility.WPF.Demo.Trees" 
             xmlns:trees="http://schemas.utility.com/trees"

             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            <system:String x:Key="Partition">QuickStart</system:String>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Utility.WPF.Controls.Trees;component/Dragablz.Controls/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </FrameworkElement.Resources>
    
    <!--<dockablz:Layout Partition="{StaticResource Partition}">
        <trees:CustomTreeViewItem Margin="8" 
                               Style="{StaticResource TabablzStyle}"
                               ShowDefaultAddButton="True"
                               ShowDefaultCloseButton="True"
                               >
            <trees:CustomTreeViewItem.InterTabController>
                <dragablz:InterTabController Partition="{StaticResource Partition}" />
            </trees:CustomTreeViewItem.InterTabController>
            <trees:CustomTreeViewItem.ItemTemplate>
                <DataTemplate>
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding }"></TextBlock>
                </DataTemplate>
            </trees:CustomTreeViewItem.ItemTemplate>
            <trees:CustomTreeViewItem.SelectedItemTemplate>
                <DataTemplate>
                    <TextBlock HorizontalAlignment="Center" FontSize="36"
                               VerticalAlignment="Center" Text="{Binding }"></TextBlock>
                </DataTemplate>
            </trees:CustomTreeViewItem.SelectedItemTemplate>

            <system:String>Hello World</system:String>
            <system:String>We Have Tearable Tabs!</system:String>
            <system:String>Tab No. 3</system:String>
            <system:String>I feel like an ice cold drink</system:String>
        </trees:CustomTreeViewItem>

    </dockablz:Layout>-->


    <!--<trees:CustomTreeViewItem Margin="8" 
                               Style="{StaticResource TabablzStyle}"
                               ShowDefaultAddButton="True"
                               ShowDefaultCloseButton="True"
                               >
    </trees:CustomTreeViewItem>-->
</UserControl>
