<UserControl x:Class="Utility.WPF.Demo.Behaviors.FadeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Utility.WPF.Demo.Behaviors"
             xmlns:b="http://schemas.utility.com/behaviors"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

            <Grid>
                <Ellipse Fill="ForestGreen" Height="100" Width="100">
                    <i:Interaction.Behaviors>
                        <b:FadeBehavior IsVisible="{Binding ElementName=ToggleButton, Path=IsChecked}" />
                    </i:Interaction.Behaviors>
                </Ellipse>
            </Grid>

        <ToggleButton Height="20" Width="70" x:Name="ToggleButton">
            Is Visible
        </ToggleButton>
    </Grid>
</UserControl>