<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:btns="http://schemas.utility.com/buttons"
    xmlns:c="http://schemas.utility.com/colours"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:res="http://schemas.utility.com/resourcedictionary"
    xmlns:sys="clr-namespace:System;assembly=netstandard"
    xmlns:inf="clr-namespace:Utility.WPF.Controls.Trees.Infrastructure"
    xmlns:trees="clr-namespace:Utility.WPF.Controls.Trees" xmlns:att="http://schemas.utility.com/attached">

    <ResourceDictionary.MergedDictionaries>
        <res:SharedResourceDictionary Source="/Utility.WPF.Controls.Trees;component/Themes/Shared.xaml" />
        <res:SharedResourceDictionary Source="/Utility.WPF.Controls.Trees;component/Themes/BasicStyle.xaml" />
        <res:SharedResourceDictionary Source="/Utility.WPF.Controls.Trees;component/Themes/ComboBoxToggleButton.xaml" />
        <res:SharedResourceDictionary Source="/Utility.WPF.Geometries;component/Resources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="ComboBoxTextBox" TargetType="{x:Type TextBox}">
        <Border
            x:Name="PART_ContentHost"
            Background="{TemplateBinding Background}"
            Focusable="False" />
    </ControlTemplate>

    <sys:Double x:Key="MaxDropDownHeight">400.0</sys:Double>

    <Style
        x:Key="ComboTreeViewItem"
        BasedOn="{StaticResource GenericTreeViewItem}"
        TargetType="{x:Type trees:CustomTreeViewItem}">
        <Style.Resources>
            <DataTemplate x:Key="EditTemplate">
                <Grid Name="rootElement" Background="#DCDCDC">
                    <TextBlock Text="no template" />
                </Grid>
            </DataTemplate>
        </Style.Resources>

        <Setter Property="SelectedItem" Value="{Binding SelectedItem, RelativeSource={RelativeSource Mode=Self}}" />
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type trees:CustomTreeViewItem}">
                    <Grid>
                        <!--<VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="MouseOver" />
                            <VisualState x:Name="Disabled">
                                <Storyboard>
                                    <ColorAnimationUsingKeyFrames Storyboard.TargetName="PART_EditableTextBox"
                                                Storyboard.TargetProperty="(TextElement.Foreground).
                      (SolidColorBrush.Color)">
                                        <EasingColorKeyFrame KeyTime="0"
                                         Value="{StaticResource DisabledForegroundColor}" />
                                    </ColorAnimationUsingKeyFrames>
                                </Storyboard>
                            </VisualState>
                        </VisualStateGroup>
                        <VisualStateGroup x:Name="EditStates">
                            <VisualState x:Name="Editable">
                                <Storyboard>
                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)"
                                                 Storyboard.TargetName="PART_EditableTextBox">
                                        <DiscreteObjectKeyFrame KeyTime="0"
                                            Value="{x:Static Visibility.Visible}" />
                                    </ObjectAnimationUsingKeyFrames>
                                    <ObjectAnimationUsingKeyFrames
                      Storyboard.TargetProperty="(UIElement.Visibility)"
                                                 Storyboard.TargetName="ContentSite">
                                        <DiscreteObjectKeyFrame KeyTime="0"
                                            Value="{x:Static Visibility.Hidden}" />
                                    </ObjectAnimationUsingKeyFrames>
                                </Storyboard>
                            </VisualState>
                            <VisualState x:Name="Uneditable" />
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>-->
                        <ToggleButton
                            x:Name="ToggleButton"
                            Grid.Column="2"
                            ClickMode="Press"
                            Focusable="false"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            Template="{StaticResource ComboBoxToggleButton}" />

                        <ContentPresenter
                            x:Name="PART_Header"
                            Margin="3,3,23,3"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="Stretch"
                            ContentSource="SelectedItem"
                            IsHitTestVisible="False" />

                        <TextBox
                            x:Name="PART_EditableTextBox"
                            Margin="3,3,23,3"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Background="Transparent"
                            Focusable="True"
                            IsReadOnly="{TemplateBinding IsReadOnly}"
                            Style="{x:Null}"
                            Template="{StaticResource ComboBoxTextBox}"
                            Visibility="Hidden" />
                        <Popup
                            x:Name="Popup"
                            AllowsTransparency="True"
                            Focusable="False"
                            IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            Placement="Bottom"
                            PopupAnimation="Slide">

                            <Grid
                                x:Name="DropDown"
                                MinWidth="{Binding ActualWidth}"
                                MaxHeight="{StaticResource MaxDropDownHeight}"
                                VerticalAlignment="Top"
                                SnapsToDevicePixels="True">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Border x:Name="DropDownBorder" BorderThickness="1">
                                    <Border.BorderBrush>
                                        <SolidColorBrush Color="{DynamicResource BorderMediumColor}" />
                                    </Border.BorderBrush>
                                    <Border.Background>
                                        <SolidColorBrush Color="{DynamicResource ControlLightColor}" />
                                    </Border.Background>
                                </Border>
                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <ItemsPresenter x:Name="ItemsHost" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>

                                <Border
                                    Grid.Row="1"
                                    Margin="4"
                                    Background="LightGray">

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <ContentPresenter x:Name="content_control" ContentSource="Edit">
                                            <FrameworkElement.Style>
                                                <Style TargetType="{x:Type ContentPresenter}">
                                                    <Setter Property="Opacity" Value="0" />
                                                    <Style.Triggers>
                                                        <!--<DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                                <Setter Property="Opacity" Value="0.5" />
                                                                </DataTrigger>-->
                                                        <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                            <!--<Setter Property="Height" Value="0" />-->
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation
                                                                            Storyboard.TargetProperty="Opacity"
                                                                            To="0"
                                                                            Duration="0:0:0.1" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <DataTrigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.ExitActions>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </FrameworkElement.Style>
                                        </ContentPresenter>

                                        <btns:GeometryToggleButton
                                            x:Name="PlusButton"
                                            Margin="2"
                                            Padding="2"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            Data="M0,1 H2 M1,0 V2"
                                            Foreground="Black">
                                            <FrameworkElement.Style>
                                                <Style TargetType="{x:Type btns:GeometryToggleButton}">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Height" Value="0" />
                                                            <Setter Property="Width" Value="0" />
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="False">
                                                            <Setter Property="Height" Value="24" />
                                                            <Setter Property="Width" Value="24" />
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </FrameworkElement.Style>
                                        </btns:GeometryToggleButton>

                                        <WrapPanel
                                            Grid.Row="1"
                                            Margin="2"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom">
                                            <FrameworkElement.Style>
                                                <Style TargetType="{x:Type WrapPanel}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                            <Setter Property="Height" Value="0" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </FrameworkElement.Style>
                                            <btns:GeometryButton
                                                x:Name="Accept_Button"
                                                Width="32"
                                                Height="32"
                                                Margin="2"
                                                Background="{c:Green 7}"
                                                Data="{StaticResource Tick_Rounded}"
                                                Foreground="White">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:ChangePropertyAction
                                                            PropertyName="IsChecked"
                                                            TargetName="PlusButton"
                                                            Value="False" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </btns:GeometryButton>
                                            <btns:GeometryButton
                                                x:Name="Decline_Button"
                                                Width="32"
                                                Height="32"
                                                Margin="2"
                                                Background="{c:Red 7}"
                                                Data="{StaticResource Cancel_Rounded}"
                                                Foreground="White">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:ChangePropertyAction
                                                            PropertyName="IsChecked"
                                                            TargetName="PlusButton"
                                                            Value="False" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </btns:GeometryButton>
                                        </WrapPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="PlusTreeViewItem"
        BasedOn="{StaticResource GenericTreeViewItem}"
        TargetType="{x:Type trees:CustomTreeViewItem}">       
        <Style.Resources>
            <DataTemplate x:Key="EditTemplate">
                <Grid Name="rootElement" Background="#DCDCDC">
                    <TextBlock Text="no template" />
                </Grid>
            </DataTemplate>
        </Style.Resources>
        <att:InteractivitySetter Property="att:InteractivityItems.Template">
            <Setter.Value>
                <att:InteractivityTemplate>
                    <att:InteractivityItems>
                        <att:InteractivityItems.Triggers>
                            <inf:CustomEventTrigger EventName="FinishEdit">
                                <inf:AddObjectAction />
                            </inf:CustomEventTrigger>
                        </att:InteractivityItems.Triggers>
                    </att:InteractivityItems>
                </att:InteractivityTemplate>
            </Setter.Value>
        </att:InteractivitySetter>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type trees:CustomTreeViewItem}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <!--<RowDefinition Height="*"></RowDefinition>-->
                        </Grid.RowDefinitions>
                        <!--<ToggleButton x:Name="ToggleButton"
                        Template="{StaticResource ComboBoxToggleButton}"
                        Grid.Column="2"
                        Focusable="false"
                        ClickMode="Press"
                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
              RelativeSource={RelativeSource TemplatedParent}}" />-->

                        <!--<ContentPresenter x:Name="PART_Header"
                                      IsHitTestVisible="False"
                                      Width="200"
                                      Margin="3,3,23,3"
                                      Content="{TemplateBinding Selection}"
                                      ContentTemplate="{TemplateBinding SelectedItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding SelectedItemTemplateSelector}"
                                      VerticalAlignment="Stretch"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />-->
                        <Grid
                            x:Name="DropDown"
                            Grid.Row="0"
                            MinWidth="{Binding ActualWidth}"
                            MaxHeight="{StaticResource MaxDropDownHeight}"
                            VerticalAlignment="Top"
                            SnapsToDevicePixels="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--<Border x:Name="DropDownBorder"
                      BorderThickness="1">
                                <Border.BorderBrush>
                                    <SolidColorBrush Color="{DynamicResource BorderMediumColor}" />
                                </Border.BorderBrush>
                                <Border.Background>
                                    <SolidColorBrush Color="{DynamicResource ControlLightColor}" />
                                </Border.Background>
                            </Border>-->
                            <Border MinHeight="100" VerticalAlignment="Stretch">
                                <!--<Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="BlueViolet"></Setter>
                                        <Setter Property="BorderBrush" Value="BlueViolet"></Setter>
                                        <Setter Property="BorderThickness" Value="7"></Setter>
                                    </Style>
                                </Border.Style>-->

                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <ItemsPresenter x:Name="ItemsHost" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>

                            <Border
                                Grid.Row="1"
                                MaxHeight="{Binding MaxHeightOfEditBox, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                Margin="4"
                                Background="LightGray">

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <ScrollViewer x:Name="PART_ScrollViewer" >

                                        <ContentPresenter x:Name="PART_ContentPresenter" Visibility="Collapsed" ContentSource="Edit">
                                            <FrameworkElement.Style>
                                                <Style TargetType="{x:Type ContentPresenter}">
                                                    <Setter Property="Opacity" Value="0" />
                                                    <Style.Triggers>
                                                        <!--<DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                                <Setter Property="Opacity" Value="0.5" />
                                                                </DataTrigger>-->
                                                        <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                            <!--<Setter Property="Height" Value="0" />-->
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation
                                                                            Storyboard.TargetProperty="Opacity"
                                                                            To="0"
                                                                            Duration="0:0:0.1" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <DataTrigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.ExitActions>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </FrameworkElement.Style>
                                        </ContentPresenter>
                                    </ScrollViewer>

                                    <Grid MinHeight="64">

                                        <btns:GeometryToggleButton
                                            x:Name="PlusButton"
                                            Grid.Row="1"
                                            Margin="2"
                                            Padding="2"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            Data="M0,1 H2 M1,0 V2"
                                            Foreground="Black">
                                            <FrameworkElement.Style>
                                                <Style TargetType="{x:Type btns:GeometryToggleButton}">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Height" Value="0" />
                                                            <Setter Property="Width" Value="0" />
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="False">
                                                            <Setter Property="Height" Value="24" />
                                                            <Setter Property="Width" Value="24" />
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </FrameworkElement.Style>
                                        </btns:GeometryToggleButton>

                                        <WrapPanel
                                            Grid.Row="1"
                                            Margin="2"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom">
                                            <FrameworkElement.Style>
                                                <Style TargetType="{x:Type WrapPanel}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                            <Setter Property="Height" Value="0" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </FrameworkElement.Style>
                                            <btns:GeometryButton
                                                x:Name="Accept_Button"
                                                Width="32"
                                                Height="32"
                                                Margin="2"
                                                Background="{c:Green 7}"
                                                Data="{StaticResource Tick_Rounded}"
                                                Foreground="White">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:ChangePropertyAction
                                                            PropertyName="IsChecked"
                                                            TargetName="PlusButton"
                                                            Value="False" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </btns:GeometryButton>
                                            <btns:GeometryButton
                                                x:Name="Decline_Button"
                                                Width="32"
                                                Height="32"
                                                Margin="2"
                                                Background="{c:Red 7}"
                                                Data="{StaticResource Cancel_Rounded}"
                                                Foreground="White">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:ChangePropertyAction
                                                            PropertyName="IsChecked"
                                                            TargetName="PlusButton"
                                                            Value="False" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </btns:GeometryButton>
                                        </WrapPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=IsChecked, ElementName=PlusButton}" Value="True">
                            <Setter TargetName="PART_ContentPresenter" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>