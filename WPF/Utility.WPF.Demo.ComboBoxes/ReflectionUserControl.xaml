<UserControl x:Class="Utility.WPF.Demo.ComboBoxes.ReflectionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Utility.WPF.Demo.ComboBoxes"
             xmlns:conv="http://schemas.utility.com/converters"
             xmlns:controls="http://schemas.utility.com/cboxes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ref="clr-namespace:System.Reflection;assembly=System.Reflection"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<FrameworkElement.Resources>
        <DataTemplate DataType="{x:Type ref:Assembly}">
            <TextBlock Text="{Binding Converter={x:Static conv:AssemblyNameConverter.Instance}}"/>
        </DataTemplate>
    </FrameworkElement.Resources>-->
    <Grid>
        <controls:ComboBoxTreeView Width="400" Height="28" >

            <i:Interaction.Behaviors>
                <local:AssemblySelectorBehavior x:Name="AssemblySelector" />
            </i:Interaction.Behaviors>
        </controls:ComboBoxTreeView>

    </Grid>
</UserControl>
