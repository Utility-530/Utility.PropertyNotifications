<Window x:Class="Utility.WPF.SandBox.AltWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:att="http://schemas.utility.com/attached"
        xmlns:local="clr-namespace:Utility.WPF.SandBox"
        mc:Ignorable="d"
        Title="AltWindow" Height="450" Width="800">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Utility.WPF.Geometries;component/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>


            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
 
            
            <Style x:Key="Horizontal" TargetType="{x:Type HeaderedContentControl}">
                <Setter Property="att:Ex.IsChecked" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value >
                        <ControlTemplate TargetType="{x:Type HeaderedContentControl}">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter ContentSource="Content"></ContentPresenter>
                                <!--<wpf1:HtmlPanel x:Name="_htmlPanel" Grid.Column="0" Grid.ColumnSpan="3" Margin="1,0,-1,0">
                                <wpf1:HtmlPanel.ToolTip>
                                    <ToolTip BorderBrush="Transparent" Padding="0">
                                        <wpf1:HtmlLabel x:Name="_htmlTooltipLabel"/>
                                    </ToolTip>
                                </wpf1:HtmlPanel.ToolTip>
                            </wpf1:HtmlPanel>-->
                                <GridSplitter x:Name="_splitter" Grid.Column="1" Width="4" VerticalAlignment="Stretch" HorizontalAlignment="Center" Background="#BFDBFF"
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

                                <ContentPresenter ContentSource="Header" Grid.Column="2"
                                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

                                    <!--<WebBrowser x:Name="_webBrowser" Grid.Column="2" 
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type local:HtmlControl}}}" />-->
                                <ToggleButton x:Name="hider" VerticalAlignment="Top" HorizontalAlignment="Right" Width="16" Grid.ColumnSpan="3" Background="Transparent" BorderThickness="0"/>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="True">
                                    <Setter Property="att:Ex.IsChecked" Value="False" />
                                    <Setter TargetName="hider" Property="Content"  >
                                        <Setter.Value>
                                            <Path Data="{StaticResource LeftArrow}" StrokeEndLineCap="Round"  Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="False">
                                    <Setter Property="att:Ex.IsChecked" Value="True" />
                                    <Setter TargetName="hider" Property="Content" >
                                        <Setter.Value>
                                            <Path Data="{StaticResource RightArrow}" StrokeEndLineCap="Round" Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="Vertical" TargetType="{x:Type HeaderedContentControl}">
                <Setter Property="att:Ex.IsChecked" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value >
                        <ControlTemplate TargetType="{x:Type HeaderedContentControl}">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <ContentPresenter ContentSource="Content"></ContentPresenter>
                                <!--<wpf1:HtmlPanel x:Name="_htmlPanel" Grid.Column="0" Grid.ColumnSpan="3" Margin="1,0,-1,0">
                                <wpf1:HtmlPanel.ToolTip>
                                    <ToolTip BorderBrush="Transparent" Padding="0">
                                        <wpf1:HtmlLabel x:Name="_htmlTooltipLabel"/>
                                    </ToolTip>
                                </wpf1:HtmlPanel.ToolTip>
                            </wpf1:HtmlPanel>-->
                                <GridSplitter x:Name="_splitter" Grid.Row="1" Height="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#BFDBFF"
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

                                <ContentPresenter ContentSource="Header" Grid.Row="2"
                                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

                                <!--<WebBrowser x:Name="_webBrowser" Grid.Column="2" 
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type local:HtmlControl}}}" />-->
                                <ToggleButton x:Name="hider" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="16" Grid.RowSpan="3" Background="Transparent" BorderThickness="0"/>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="True">
                                    <Setter Property="att:Ex.IsChecked" Value="False" />
                                    <Setter TargetName="hider" Property="Content"  >
                                        <Setter.Value>
                                            <Path Data="{StaticResource UpArrow}" StrokeEndLineCap="Round"  Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="False">
                                    <Setter Property="att:Ex.IsChecked" Value="True" />
                                    <Setter TargetName="hider" Property="Content" >
                                        <Setter.Value>
                                            <Path Data="{StaticResource DownArrow}" StrokeEndLineCap="Round" Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
     
    </FrameworkElement.Resources>
    <UniformGrid Columns="1">

        <HeaderedContentControl Style="{StaticResource Horizontal}">
            <HeaderedContentControl.Header>
                <Ellipse Fill="Red" Height="20" Width="20"></Ellipse>
       
            </HeaderedContentControl.Header>
            <Ellipse Fill="Blue" Height="20" Width="20"></Ellipse>
        </HeaderedContentControl>  
        
        <HeaderedContentControl Style="{StaticResource Vertical}">
            <HeaderedContentControl.Header>
                <Ellipse Fill="Red" Height="20" Width="20"></Ellipse>
       
            </HeaderedContentControl.Header>
            <Ellipse Fill="Blue" Height="20" Width="20"></Ellipse>
        </HeaderedContentControl>
        
    </UniformGrid>
</Window>
