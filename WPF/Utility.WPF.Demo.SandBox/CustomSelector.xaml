<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Utility.WPF.Demo.SandBox"
                    xmlns:local2="clr-namespace:Utility.WPF.SandBox"
                    xmlns:btns="http://schemas.utility.com/buttons"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:res="http://schemas.utility.com/resourcedictionary"
                    xmlns:c="http://schemas.utility.com/colours"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
                    xmlns:ext="http://schemas.utility.com/external" 
                    xmlns:att="http://schemas.utility.com/attached" 
                    xmlns:beh="http://schemas.utility.com/behaviors" 
                    xmlns:obj="http://schemas.utility.com/objects" 
                    xmlns:pt="clr-namespace:Utility.WPF.Controls.PropertyTrees;assembly=Utility.WPF.Controls.PropertyTrees">

    <ResourceDictionary.MergedDictionaries>
        <res:SharedResourceDictionary Source="/Utility.WPF.Geometries;component/Resources.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <local2:CheckedEventArgsConverter x:Key="CheckedEventArgsConverter"/>

    <Style x:Key="ButtonFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <LinearGradientBrush x:Key="ButtonNormalBackground" EndPoint="0,1" StartPoint="0,0">
        <GradientStop Color="#F3F3F3" Offset="0"/>
        <GradientStop Color="#EBEBEB" Offset="0.5"/>
        <GradientStop Color="#DDDDDD" Offset="0.5"/>
        <GradientStop Color="#CDCDCD" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ButtonNormalBorder" Color="#FF707070"/>

    <SolidColorBrush x:Key="CheckBoxFillNormal" Color="#F4F4F4"/>
    <SolidColorBrush x:Key="CheckBoxStroke" Color="#8E8F8F"/>
    <Style x:Key="EmptyCheckBoxFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="1" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CheckRadioFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="14,0,0,0" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CheckBoxPingUnPing" TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="Background" Value="{StaticResource CheckBoxFillNormal}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CheckBoxStroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Width" Value="20"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource EmptyCheckBoxFocusVisual}"/>


        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Border x:Name="border" BorderBrush="White" BorderThickness="4" CornerRadius="4">
                        <Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="Black" Offset="0"/>
                                <GradientStop Color="Black" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Checked">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[0].(GradientStop.Color)" Storyboard.TargetName="border">
                                            <EasingColorKeyFrame KeyTime="0" Value="#FF0073FD"/>
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[1].(GradientStop.Color)" Storyboard.TargetName="border">
                                            <EasingColorKeyFrame KeyTime="0" Value="#FF005BC2"/>
                                        </ColorAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[1].(GradientStop.Offset)" Storyboard.TargetName="border">
                                            <EasingDoubleKeyFrame KeyTime="0" Value="0.742"/>
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(GradientBrush.GradientStops)[0].(GradientStop.Offset)" Storyboard.TargetName="border">
                                            <EasingDoubleKeyFrame KeyTime="0" Value="0.143"/>
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Unchecked"/>
                                <VisualState x:Name="Indeterminate"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <att:InteractivitySetter>
            <Setter.Value>
                <att:InteractivityTemplate>
                    <att:InteractivityItems>
                        <att:InteractivityItems.Triggers>
                            <i:EventTrigger EventName="Checked">
                                <beh:InvokeCommandAction EventArgsConverter="{StaticResource CheckedEventArgsConverter}" Command="{Binding CheckedCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
                            </i:EventTrigger>
                            <i:EventTrigger EventName="Unchecked">
                                <beh:InvokeCommandAction EventArgsConverter="{StaticResource CheckedEventArgsConverter}" Command="{Binding CheckedCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
                            </i:EventTrigger>
                        </att:InteractivityItems.Triggers>
                    </att:InteractivityItems>
                </att:InteractivityTemplate>
            </Setter.Value>
        </att:InteractivitySetter>
    </Style>

    <Style x:Key="ButtonClose" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
        <Setter Property="Background" Value="{StaticResource ButtonNormalBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Width" Value="20"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Command" Value="{Binding RemoveCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="CommandParameter" Value="{Binding DataContext, RelativeSource={RelativeSource Self}}"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border BorderBrush="Transparent" BorderThickness="4" Background="Black" CornerRadius="3">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver"/>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="rectangle">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Unfocused"/>
                                <VisualState x:Name="Focused"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="ValidationStates">
                                <VisualState x:Name="Valid"/>
                                <VisualState x:Name="InvalidFocused"/>
                                <VisualState x:Name="InvalidUnfocused"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid>
                            <Path Data="M14.833333,10 L29,21.166667" Fill="#FFF4F4F5" Height="Auto" Stretch="Fill" Stroke="#FFFFFEFE" VerticalAlignment="Stretch" StrokeThickness="4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                            <Path Data="M7.041,20.209 L18.6875,5.75" Fill="#FFF4F4F5" HorizontalAlignment="Stretch" Stretch="Fill" Stroke="#FFFFFEFE" Width="Auto" StrokeThickness="4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                            <Rectangle x:Name="rectangle" Fill="#FF717172" Height="Auto" Stroke="#FFFFFEFE" StrokeThickness="0" VerticalAlignment="Stretch" Width="Auto" Opacity="0.485" RadiusX="0" RadiusY="0" Visibility="Hidden" ClipToBounds="True"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="true"/>
                        <Trigger Property="ToggleButton.IsChecked" Value="true"/>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="#ADADAD"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>




    <Style x:Key="ListBoxItemStyle1" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Background">
            <Setter.Value>
                <RadialGradientBrush GradientOrigin="0.505,1.227" RadiusY="0.619" RadiusX="0.646" Center="0.508,0.678">
                    <RadialGradientBrush.RelativeTransform>
                        <TransformGroup>
                            <ScaleTransform CenterY="0.5" CenterX="0.5" ScaleY="1" ScaleX="1"/>
                            <SkewTransform AngleY="0" AngleX="0" CenterY="0.5" CenterX="0.5"/>
                            <RotateTransform Angle="-180.156" CenterY="0.5" CenterX="0.5"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </RadialGradientBrush.RelativeTransform>
                    <GradientStop Color="#FF333333" Offset="0.039"/>
                    <GradientStop Color="#FF292828" Offset="0.836"/>
                    <GradientStop Color="#FF333333" Offset="0.36"/>
                </RadialGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="Padding" Value="2,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border Background="{TemplateBinding Background}">
                        <Grid>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          RecognizesAccessKey="True"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                            <Button Style="{StaticResource ButtonClose}" HorizontalAlignment="Right" Height="20" Width="20"  VerticalAlignment="Top" />
                            <CheckBox Style="{StaticResource CheckBoxPingUnPing}" HorizontalAlignment="Right"  VerticalAlignment="Bottom"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type local:CustomSelector}" >
        <Style.Resources>
            <DataTemplate x:Key="EditTemplate">
                <!--<Grid Background="#DCDCDC" Name="rootElement">-->
                    <!--<obj:JsonControl Object="{Binding Path=., Mode=TwoWay, Converter={StaticResource ObjectToTokenConverter}}">
                        --><!--<i:Interaction.Behaviors>
                            <local:ChangeBehavior></local:ChangeBehavior>
                        </i:Interaction.Behaviors>--><!--
                    </obj:JsonControl>-->
                <!--</Grid>-->
                <pt:PropertyTree Object="{Binding Path=.}"></pt:PropertyTree> 
            </DataTemplate>
        </Style.Resources>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <ext:DraggAnimatedPanel ItemSeparation="2" ItemsHeight="140" ItemsWidth="220" 
                                            Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}" 
                                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}"
                                            SwapCommand="{Binding SwapCommand, RelativeSource={RelativeSource AncestorType={x:Type local:CustomSelector}}}"/>
                </ItemsPanelTemplate>
            </Setter.Value>  
        </Setter>
        <Setter Property="SnapsToDevicePixels"          Value="true" />
        <Setter Property="OverridesDefaultStyle"
          Value="true" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
          Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"
          Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll"
          Value="true" />
        <Setter Property="MinWidth"
          Value="120" />
        <Setter Property="MinHeight"
          Value="20" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource ListBoxItemStyle1}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomSelector}">
                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                            <!--<RowDefinition Height="*"></RowDefinition>-->
                        </Grid.RowDefinitions>
                        <!--<ToggleButton x:Name="ToggleButton"
                        Template="{StaticResource ComboBoxToggleButton}"
                        Grid.Column="2"
                        Focusable="false"
                        ClickMode="Press"
                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, 
              RelativeSource={RelativeSource TemplatedParent}}"/>-->

                        <!--<ContentPresenter x:Name="PART_Header"
                                      IsHitTestVisible="False"
                                      Width="200"
                                      Margin="3,3,23,3"
                                      Content="{TemplateBinding Selection}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding SelectionBoxTemplateSelector}"
                                      VerticalAlignment="Stretch"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>-->



                        <Grid x:Name="DropDown" 
                              Grid.Row="1" 
                              VerticalAlignment="Top"
                              SnapsToDevicePixels="True"                        
                              MinWidth="{Binding ActualWidth}"
                              >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>

                            <!--<Border x:Name="DropDownBorder"
                      BorderThickness="1">
                                <Border.BorderBrush>
                                    <SolidColorBrush Color="{DynamicResource BorderMediumColor}" />
                                </Border.BorderBrush>
                                <Border.Background>
                                    <SolidColorBrush Color="{DynamicResource ControlLightColor}" />
                                </Border.Background>
                            </Border>-->

                            <!--<ScrollViewer Margin="4,6,4,6" VerticalScrollBarVisibility="Visible"
                                          SnapsToDevicePixels="True">
                                <ItemsPresenter x:Name="ItemsHost"
                                                KeyboardNavigation.DirectionalNavigation="Contained" />
                            </ScrollViewer>-->
                            <ext:AnimatedScrollViewer
								x:Name="PART_AnimatedScrollViewer"
								CanKeyboardScroll="False"                                
								Padding="{TemplateBinding Padding}"
								Focusable="false"
								ScrollingSpline="0,0.50,0.50,1">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ext:AnimatedScrollViewer>

                            <Border x:Name="Border" Grid.Row="1" Background="LightGray" Margin="4">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition x:Name="row1" Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ContentControl x:Name="content_control" 
                                                    Content="{TemplateBinding NewObject}"       
                                                    ContentTemplate="{TemplateBinding EditTemplate}"
                                                    ContentTemplateSelector="{TemplateBinding EditTemplateSelector}">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type ContentControl}">
                                                <Setter Property="Opacity" Value="0"></Setter>
                                                <Style.Triggers>
                                                    <!--<DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                                <Setter Property="Opacity" Value="0.5" />
                                                                </DataTrigger>-->
                                                    <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                        <!--<Setter Property="Height" Value="0" />-->
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.1"></DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1"></DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </FrameworkElement.Style>
                                    </ContentControl>

                                    <btns:GeometryToggleButton x:Name="PlusButton"                                   
                                            Data="{StaticResource Plus}"
                                            Foreground="Black"      
                                            Width="{Binding ElementName=Border, Path=ActualWidth}"
                                            Grid.Row="1"                                                                           
                                            HorizontalAlignment="Stretch" 
                                            VerticalAlignment="Stretch" Margin="2" Padding="2">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type btns:GeometryToggleButton}">
                                                <Style.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Height" Value="0" />
                                                        <Setter Property="Width" Value="0" />
                                                    </Trigger>
                                                    <Trigger Property="IsChecked" Value="False">
                                                        <Setter Property="Height" Value="24" />
                                                        <Setter Property="Width" Value="24" />
                                                    </Trigger>

                                                 
                                                </Style.Triggers>
                                            </Style>

                                        </FrameworkElement.Style>
                                    </btns:GeometryToggleButton>


                                    <WrapPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.Row="1" Margin="2">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type WrapPanel}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=PlusButton, Path=IsChecked}" Value="False">
                                                        <Setter Property="Height" Value="0" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </FrameworkElement.Style>
                                        <btns:GeometryButton x:Name="Accept_Button" Data="{StaticResource Tick_Rounded}"   
                                                                         Width="32" Margin="2"
                                                                         Foreground="White"
                                                                         Background="{c:Green 7}"
                                                                         Height="32" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:ChangePropertyAction TargetName="PlusButton" PropertyName="IsChecked" Value="False"></i:ChangePropertyAction>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </btns:GeometryButton>
                                        <btns:GeometryButton x:Name="Decline_Button"   Data="{StaticResource Cancel_Rounded}"   
                                                                         Width="32" Margin="2"
                                                                         Foreground="White"
                                                                         Background="{c:Red 7}"
                                                                         Height="32" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:ChangePropertyAction TargetName="PlusButton" PropertyName="IsChecked" Value="False"></i:ChangePropertyAction>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </btns:GeometryButton>
                                    </WrapPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=IsChecked, ElementName=PlusButton}" Value="False">
                            <Setter TargetName="content_control" Property="Height" Value="0" />        
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>