<Window x:Class="Utility.WPF.SandBox.ButtonsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Utility.WPF.SandBox" xmlns:view="http://schemas.utility.com/buttons" xmlns:math="http://hexinnovation.com/math"
        mc:Ignorable="d"
        Title="ButtonsWindow" Height="450" Width="800">
    <FrameworkElement.Resources>
        <math:MathConverter x:Key="MathConverter"></math:MathConverter>

        <Style x:Key="CloseListBoxItemStyle" 
               TargetType="{x:Type ContentControl}" 
               BasedOn="{StaticResource {x:Type ContentControl}}">
            <Setter Property="Height" Value="80"></Setter>
            <Setter Property="FontSize" Value="14"></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Border Name="border"
                                        VerticalAlignment="Stretch"
                                        HorizontalAlignment="Stretch"
                                            Background="{TemplateBinding Background}"
                                            BorderThickness="1"
                                            BorderBrush="LightGray"
                                            CornerRadius="5" Margin="2">
                            <Grid>
                                <ContentPresenter VerticalAlignment="Center" Margin="8,0,4,0" />
                                <view:DuplicateGeometryButton  Height="{Binding ElementName=border, Path=ActualHeight, ConverterParameter=x/2, Converter={StaticResource MathConverter}}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0" />
                                <view:XGeometryButton Height="{Binding ElementName=border, Path=ActualHeight, ConverterParameter=x/3, Converter={StaticResource MathConverter}}" VerticalAlignment="Top" HorizontalAlignment="Right" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="Background" Value="WhiteSmoke" />
                    <Setter Property="Cursor" Value="Hand" />
                </Trigger>
                <!--<Trigger Property="IsSelected" Value="true">
                    <Setter Property="Background" Value="LightGray" />
                    <Setter Property="BorderBrush" Value="DarkGray" />
                </Trigger>-->
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Opacity" Value="0.5" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </FrameworkElement.Resources>
    <Grid>
        <ContentControl Style="{StaticResource CloseListBoxItemStyle}" >

            SOME CONTENT
        </ContentControl>
    </Grid>
</Window>


