<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pt1="clr-namespace:PropertyTools.Wpf"
    xmlns:local="clr-namespace:Utility.WPF.Controls.Base"
    xmlns:enms="clr-namespace:Utility.Enums;assembly=Utility.Enums"
    xmlns:att="http://schemas.utility.com/attached"
    xmlns:res="http://schemas.utility.com/resourcedictionary">
    <ResourceDictionary.MergedDictionaries>
        <res:SharedResourceDictionary Source="/Utility.WPF.Geometries;component/Resources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <ControlTemplate  x:Key="LeftToRight" TargetType="{x:Type HeaderedItemsControl}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ItemsPresenter />

            <GridSplitter x:Name="_splitter" Grid.Column="1" Width="4" VerticalAlignment="Stretch" HorizontalAlignment="Center" Background="#BFDBFF"
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

            <ContentPresenter ContentSource="Header" Grid.Column="2" x:Name="PART_HeaderPresenter"
                                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

            <ToggleButton x:Name="hider" VerticalAlignment="Top" HorizontalAlignment="Right" Width="16" Grid.ColumnSpan="3" Background="Transparent" BorderThickness="0" />
        </Grid>

        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="True">
                <Setter Property="att:Ex.IsChecked" Value="False" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource LeftArrow}" StrokeEndLineCap="Round"  Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="False">
                <Setter Property="att:Ex.IsChecked" Value="True" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource RightArrow}" StrokeEndLineCap="Round" Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate  x:Key="RightToLeft" TargetType="{x:Type HeaderedItemsControl}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ContentPresenter ContentSource="Header" x:Name="PART_HeaderPresenter"
                                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

            <GridSplitter x:Name="_splitter" Grid.Column="1" Width="4" VerticalAlignment="Stretch" HorizontalAlignment="Center" Background="#BFDBFF"
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

            <ItemsPresenter  Grid.Column="2"  ></ItemsPresenter>
            <ToggleButton x:Name="hider" VerticalAlignment="Top" HorizontalAlignment="Left" Width="16" Grid.ColumnSpan="3" Background="Transparent" BorderThickness="0" />
        </Grid>

        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="True">
                <Setter Property="att:Ex.IsChecked" Value="False" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource LeftArrow}" StrokeEndLineCap="Round"  Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="False">
                <Setter Property="att:Ex.IsChecked" Value="True" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource RightArrow}" StrokeEndLineCap="Round" Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="TopToBottom" TargetType="{x:Type HeaderedItemsControl}">

        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ItemsPresenter></ItemsPresenter>

            <GridSplitter x:Name="_splitter" Grid.Row="1" Height="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#BFDBFF"
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

            <ContentPresenter ContentSource="Header" Grid.Row="2"  x:Name="PART_HeaderPresenter"
                                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

            <ToggleButton x:Name="hider" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="16" Grid.RowSpan="3" Background="Transparent" BorderThickness="0" />
        </Grid>

        <ControlTemplate.Triggers>

            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="True">
                <Setter Property="att:Ex.IsChecked" Value="False" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource UpArrow}" StrokeEndLineCap="Round"  Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="False">
                <Setter Property="att:Ex.IsChecked" Value="True" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource DownArrow}" StrokeEndLineCap="Round" Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="BottomToTop" TargetType="{x:Type HeaderedItemsControl}">

        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ContentPresenter ContentSource="Header" Grid.Row="0" x:Name="PART_HeaderPresenter"
                                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />

            <GridSplitter x:Name="_splitter" Grid.Row="1" Height="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#BFDBFF"
                                    Visibility="{Binding Path=(att:Ex.IsChecked), Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type HeaderedContentControl}}}" />
            <ItemsPresenter Grid.Row="2"></ItemsPresenter>
            <ToggleButton x:Name="hider" VerticalAlignment="Top" HorizontalAlignment="Left" Width="16" Grid.RowSpan="3" Background="Transparent" BorderThickness="0" />
        </Grid>

        <ControlTemplate.Triggers>

            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="True">
                <Setter Property="att:Ex.IsChecked" Value="False" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource UpArrow}" StrokeEndLineCap="Round"  Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsChecked, ElementName=hider}" Value="False">
                <Setter Property="att:Ex.IsChecked" Value="True" />
                <Setter TargetName="hider" Property="Content">
                    <Setter.Value>
                        <Path Data="{StaticResource DownArrow}" StrokeEndLineCap="Round" Stretch="Uniform" Stroke="Black" StrokeThickness="2"></Path>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type local:SplitItemsControl}" BasedOn="{StaticResource {x:Type HeaderedItemsControl}}">
        <Setter Property="att:Ex.IsChecked" Value="False" />
        <Setter Property="Background" Value="Transparent"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SplitItemsControl}">
                    <DockPanel x:Name="PART_DockPanel"
                               LastChildFill="True"
                               Background="{TemplateBinding Background}">

                        <ContentPresenter ContentSource="Header" x:Name="PART_HeaderPresenter" />

                        <ItemsPresenter  x:Name="PART_ItemsPresenter" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Movement" Value="{x:Static enms:XYTraversal.BottomToTop}">
                <Setter Property="Template"  Value="{StaticResource BottomToTop}" />
            </Trigger>
            <Trigger Property="Movement" Value="{x:Static enms:XYTraversal.TopToBottom}">
                <Setter Property="Template"  Value="{StaticResource TopToBottom}" />
            </Trigger>
            <Trigger Property="Movement" Value="{x:Static enms:XYTraversal.RightToLeft}">
                <Setter Property="Template"  Value="{StaticResource RightToLeft}" />
            </Trigger>
            <Trigger Property="Movement" Value="{x:Static enms:XYTraversal.LeftToRight}">
                <Setter Property="Template"  Value="{StaticResource LeftToRight}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>