<rui:ReactiveUserControl
    x:TypeArguments="viewmodel:DemoFactoryViewModel"
    x:Class="Utility.Tasks.DemoApp.View.FactoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:rui="http://reactiveui.net"
    xmlns:viewmodel="clr-namespace:Utility.Tasks.DemoApp.ViewModel"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">

    <FrameworkElement.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="100"></Setter>
            <Setter Property="Height" Value="30"></Setter>
        </Style>
    </FrameworkElement.Resources>
    <DockPanel>
        <UniformGrid Rows="1" DockPanel.Dock="Top" Margin="10">
            <Button x:Name="StandardButton">Standard</Button>
            <Button x:Name="DelayedButton">Delayed</Button>
        </UniformGrid>
        <rui:ViewModelViewHost x:Name="MainViewModelViewHost" 
                               VerticalContentAlignment="Stretch" 
                               HorizontalContentAlignment="Stretch">
            
        </rui:ViewModelViewHost>
    </DockPanel>
</rui:ReactiveUserControl>
